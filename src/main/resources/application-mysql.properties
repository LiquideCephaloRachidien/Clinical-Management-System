#properties file for activating MySQL database (MySQL database must be locally created first)


spring.datasource.url=jdbc:mysql://localhost:3306/patient_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=${JDBC_DATABASE_USERNAME:root}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update